import { AUTH_CONTRACT_HEADER, AUTH_CONTRACT_VERSION, DESKTOP_ATTEMPT_PATH, DESKTOP_COMPLETE_PATH } from "./contract";
import { isDesktopCallbackProtocol, isDesktopCode } from "./desktop-handoff";
export class BrokerApiError extends Error { constructor(public readonly status: number) { super(`Auth broker request failed (${status})`); } }
type Input = { state: string; code_challenge: string; local?: boolean };
export async function registerDesktopGoogleAttempt(input: Input): Promise<void> { const response = await post(DESKTOP_ATTEMPT_PATH, input); const payload: unknown = await response.json(); if (!payload || typeof payload !== "object" || (payload as Record<string, unknown>).registered !== true) throw new BrokerApiError(502); }
export async function completeDesktopGoogleAttempt(sessionToken: string, input: Input): Promise<{ callbackProtocol: string; code: string }> { const response = await post(DESKTOP_COMPLETE_PATH, input, sessionToken); const payload: unknown = await response.json(); const value = payload && typeof payload === "object" ? payload as Record<string, unknown> : {}; if (!isDesktopCode(value.code) || !isDesktopCallbackProtocol(value.callback_protocol)) throw new BrokerApiError(502); return { callbackProtocol: value.callback_protocol, code: value.code }; }
async function post(path: string, body: Input, token?: string): Promise<Response> { const headers = new Headers({ "content-type": "application/json", [AUTH_CONTRACT_HEADER]: String(AUTH_CONTRACT_VERSION) }); if (token) headers.set("authorization", `Bearer ${token}`); const response = await fetch(path, { method: "POST", headers, body: JSON.stringify(body), credentials: "same-origin", cache: "no-store", redirect: "error" }); if (!response.ok) throw new BrokerApiError(response.status); return response; }
