export type GoogleSsoParams = { strategy: "oauth_google"; redirectUrl: string; redirectCallbackUrl: string; oidcPrompt: "select_account" };
type Attempt = { status?: string | null; isTransferable?: boolean; existingSession?: { sessionId?: string } | null };
function record(value: unknown): Record<string, unknown> | null { return typeof value === "object" && value !== null ? value as Record<string, unknown> : null; }
export function readGoogleSso(signIn: unknown): ((params: GoogleSsoParams) => Promise<{ error: unknown }>) | null { const value = record(signIn); if (!value) return null; if (typeof value.sso === "function") return value.sso.bind(value) as (params: GoogleSsoParams) => Promise<{ error: unknown }>; const future = record(value.__internal_future); return future && typeof future.sso === "function" ? future.sso.bind(future) as (params: GoogleSsoParams) => Promise<{ error: unknown }> : null; }
export async function startGoogleOAuth(signIn: unknown, origin: string, query: string) { const sso = readGoogleSso(signIn); if (!sso) throw new Error("Google sign-in is unavailable"); return sso({ strategy: "oauth_google", redirectUrl: new URL(`/login?${query}`, origin).href, redirectCallbackUrl: new URL(`/oauth/google/callback?${query}`, origin).href, oidcPrompt: "select_account" }); }
export function hasClerkOAuthReturn(params: { has(name: string): boolean }, hash: string): boolean { return params.has("rotating_token_nonce") || params.has("__clerk_status") || params.has("__clerk_ticket") || /__clerk/i.test(hash); }
export function googleOAuthAttemptIsReady(signIn: Attempt | null | undefined, signUp: Attempt | null | undefined): boolean { return Boolean(signIn && signUp && (signIn.status === "complete" || signUp.status === "complete" || signIn.isTransferable || signUp.isTransferable || signIn.existingSession?.sessionId || signUp.existingSession?.sessionId || signIn.status)); }
export async function consumeGoogleOAuthNonce(signIn: unknown, nonce: string | null): Promise<boolean> { if (!nonce) return true; const value = record(signIn); const future = value ? record(value.__internal_future) : null; const reload = typeof value?.reload === "function" ? value.reload.bind(value) : typeof future?.reload === "function" ? future.reload.bind(future) : null; if (!reload) return false; await (reload as (input: { rotatingTokenNonce: string }) => Promise<unknown>)({ rotatingTokenNonce: nonce }); return true; }
