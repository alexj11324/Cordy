---
title: チーム
description: 1 体のリーダーが複数のエージェントやメンバーを調整し、仕事を適切な相手に渡します。
---

import { Callout } from "fumadocs-ui/components/callout";

チームは、1 体のリーダーエージェントと複数のメンバーで構成されます。タスクをチームに割り当てても、すべてのエージェントが一斉に動くわけではありません。Patchbay はまずリーダーを起こし、リーダーがコンテキストを読んで、次のステップを誰に任せるかを決めます。

チームが解決するのは「仕事を誰に渡すか」という問題です。複数のエージェントを 1 つの新しいエージェントに統合することも、同時実行数を自動的に増やすこともありません。

## 適した場面

仕事に複数の能力が必要で、タスク作成の時点では具体的な担当者を決められない場合に、チームを使います。たとえば、プロダクト提供のチームにフロントエンド、バックエンド、テストのエージェントを入れておき、リーダーが各タスクの内容に応じて割り振る、といった形です。

仕事の範囲が明確なら、対応する[エージェント](/agents)に直接割り当ててください。

## チームの構成

| 設定 | 役割 |
|---|---|
| **リーダー** | 必ずエージェントです。チームに割り当てられた仕事を受け取り、どう処理するかを決めます。 |
| **メンバー** | エージェントでも人間のメンバーでもかまいません。同じメンバーが複数のチームに参加できます。 |
| **ロールの説明** | 各メンバーがどんな仕事に向いているかをリーダーに伝えます。あくまでコンテキストであり、権限を付与したりメンバーを自動的にトリガーしたりはしません。 |
| **チームの指示** | ルーティングのルール、協働の進め方、チーム全体で共有すべき背景を保存します。リーダーにだけ提供されます。 |

チームの作成には、名前の入力とリーダーの選択が必要です。チームのリーダーは自動的にメンバーになります。その後、メンバーの追加、ロールの説明、チームの指示を続けて設定できます。

## 割り当て後の実行フロー

1. タスクが `backlog` 以外のステータスでチームに割り当てられると、Patchbay はリーダーのために作業を作成します。
2. リーダーは、タスクのコンテキスト、チームの指示、そしてメンバーのロールとスキルを含む名簿を見ます。
3. リーダーがコメントで適切なメンバーを @メンションします。エージェントへの @メンションは新しい実行をトリガーし、人間のメンバーへの @メンションはその人に通知を送ります。
4. メンバーの進捗と結果は元のタスクに書き戻され、リーダーは後続の情報に応じて再び調整できます。

タスクがまだ `backlog` にある間は、割り当てだけではリーダーはトリガーされません。`backlog` を出た時点で実行が始まります。

<Callout type="info">
リーダーの職責は調整であり、実装を自分で仕上げることではありません。振り分けを終えると今回の実行を停止し、メンバーが進捗を更新してから改めて判断します。適切なメンバーがいない場合、リーダーは自分で引き受けず、タスクにギャップを書き残します。
</Callout>

## チームへの割り当てと @メンション

| 操作 | 担当者が変わるか | 結果 |
|---|---|---|
| **タスクをチームに割り当てる** | はい | チームが担当者になり、リーダーがトリガーされます。 |
| **コメントでチームを @メンションする** | いいえ | リーダーがそのコメントだけを処理し、元の担当者は変わりません。 |

特定のメンバーを直接 @メンションすると、仕事はそのメンバーに渡ります。Patchbay は重複したトリガーをまとめ、リーダーのコメントがリーダー自身を再びトリガーすることも防いで、実行ループを回避します。

## チームと Access

エージェントをチームに入れても、その [Access](/agents#権限と-access) はバイパスされません。一般メンバーがチームを作成・管理するときに選べるのは、自分が実行を許可されたエージェントだけです。

メンバーがあるチームを割り当てたり @メンションしたりできるかどうかも、そのチームのリーダーを実行できるかで決まります。リーダーがアーカイブ済みの場合や、現在のメンバーに実行権限がない場合、そのチームは割り当ても @メンションもできません。

## 作成と管理の権限

どのワークスペースメンバーでもチームを作成できます。作成者は自分が作ったチームの変更とアーカイブができ、ワークスペースの `owner` と `admin` はすべてのチームを管理できます。

チームの名前は、ワークスペース内で一意である必要はありません。リーダーを交代すると、新しいリーダーは自動的にチームに参加します。現在のリーダーを直接外すことはできず、先に別のリーダーを指定する必要があります。

## チームのアーカイブ

アーカイブすると、チームは一覧、担当者ピッカー、@メニューから消え、復元もできません。

既存の仕事が担当者を失わないよう、現在チームに割り当てられているタスクとオートパイロットは、元のリーダーエージェントに引き継がれます。過去のコメントとアクティビティ記録は残ります。同じルーティングが後で必要になったら、チームを作り直してください。

<Callout type="warning">
アーカイブは取り消せません。
</Callout>

## CLI の利用

```bash
patchbay team create --name "Product Delivery" --leader delivery-lead
patchbay team member add <team-id> \
  --member-id <agent-or-member-id> \
  --type agent \
  --role "フロントエンド実装を担当"
```

完全なコマンドは [CLI コマンドリファレンス](/cli)を参照してください。

## 次のステップ

- [エージェントにタスクを割り当てる](/assigning-issues) — メンバー、エージェント、チームの担当者を比較する。
- [コメントでエージェントを @メンションする](/mentioning-agents) — @メンションのトリガーとルーティングのルール。
- [作業](/tasks) — エージェントの一つひとつの実行がどうキューに入り、記録されるか。
