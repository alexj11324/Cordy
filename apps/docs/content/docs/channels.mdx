---
title: Chat integrations
description: Connect Patchbay agents to Feishu, Lark, Slack, DingTalk, WeCom, or Telegram and use them from the chat tools your team already uses.
---

import { Callout } from "fumadocs-ui/components/callout";

Chat integrations let the team ask an agent questions, @-mention it in a group chat, or create issues from the chat window — without opening Patchbay.

Feishu/Lark, Slack, DingTalk, WeCom, and Telegram are supported today. They share the same session, identity, and execution mechanics but install differently.

## Choose a platform

| | Feishu / Lark | Slack | DingTalk | WeCom | Telegram |
|---|---|---|---|---|---|
| Installation | Generate a QR code in Patchbay and authorize by scanning it with Feishu | Create an app in Slack, then paste two tokens into Patchbay | Create an internal app and Stream-mode robot, then paste its AppKey and AppSecret into Patchbay | Create a smart bot with the long connection enabled in the WeCom admin console, then paste its Bot ID and Secret into Patchbay | Create a Bot with @BotFather, then paste its token into Patchbay |
| DM the agent | Supported | Supported | Supported | Supported | Supported |
| Groups or channels | Triggered by @-mentioning the Bot | Triggered by @-mentioning the Bot | Triggered by @-mentioning the Bot | Triggered by @-mentioning the Bot | Triggered by @-mentioning or replying to the Bot |
| Create issues | `/issue` message command; creates the issue from your input as-is | `/issue` slash command; the agent writes up the description before creating | `/issue` message command; creates the issue from your input as-is | `/issue` message command; creates the issue from your input as-is | `/issue` message command; creates the issue from your input as-is |
| Start a new Chat | `/new [message]` | DM: `/new [message]`; channel/thread: `@Patchbay /new [message]` | `/new [message]` | `/new [message]` | `/new [message]` |
| Clear the current Chat context | `/clear [message]` | DM: `/clear [message]`; channel/thread: `@Patchbay /clear [message]` | `/clear [message]` | `/clear [message]` | `/clear [message]` |
| Connection | Platform long connection | Socket Mode | Stream mode | Platform long connection | `getUpdates` long polling |

New connections are currently open only for mainland-China Feishu; existing international Lark connections keep working and can still be managed.

Each Bot is bound to one Patchbay agent. To use several agents on the same chat platform, connect a separate Bot for each.

DingTalk, WeCom, and Telegram are [community-maintained](/community-maintained): they ship in every release, but carry no official support SLA. Report problems in [GitHub issues](https://github.com/patchbay-ai/patchbay/issues).

WeCom handles text messages today. Voice, image, and file messages get a short reply explaining that, and are not passed to the agent.

Telegram handles text messages today. Unsupported media gets a short reply explaining that, and is not passed to the agent.

Step-by-step guides:

- [Feishu / Lark Bot integration](/lark-bot-integration)
- [Slack Bot integration](/slack-bot-integration)
- [DingTalk Bot integration](/dingtalk-bot-integration)
- [Telegram Bot integration](/telegram-bot-integration)

## How a message is processed

1. Patchbay resolves the Bot to its workspace and agent.
2. In a group or channel, only messages that explicitly @-mention the Bot continue; DMs need no mention.
3. Patchbay verifies the sender's account binding and workspace membership.
4. The message joins an [agent chat](/chat) session, and a task is created.
5. The agent's reply goes back to the original DM or thread.

Channel messages that don't @-mention the Bot never trigger the agent and are not added to its conversation context.

Ordinary messages follow this flow. `/issue` is a command, not a chat turn: Patchbay posts the result back to the originating platform but does not add the command to Patchbay Chat. Slack's native slash command continues through its separate asynchronous issue-creation flow.

## Conversation controls

`/new` creates a new Patchbay Chat and routes later messages from that external conversation to it. `/new <message>` creates the Chat and uses the message as its first turn. The previous Chat remains stored and usable in Patchbay.

`/clear` stays in the current Patchbay Chat but starts a new agent-visible context. The full Chat history remains available in Patchbay, while the agent cannot retrieve messages from before the boundary. `/clear <message>` uses the message as the first turn in the new context; a bare `/clear` applies to the next real message.

On Slack, `/new` and `/clear` are native slash commands in a DM. A native slash-command payload does not identify a channel thread, so use `@Patchbay /new [message]` or `@Patchbay /clear [message]` inside the target thread.

## Session isolation

- Feishu/Lark separates sessions by chat; later messages in the same chat continue the same session.
- Slack separates DMs by channel; in a channel, each thread holds its own session.
- DingTalk separates sessions by conversation; each DM or group continues its own session.
- WeCom separates sessions by chat; each DM or group chat continues its own session.
- Telegram separates sessions by chat; forum topics are isolated by topic.

Follow-ups in a channel still need a fresh @-mention. The agent only receives messages addressed to it — it never reads the whole channel history automatically.

## Account binding

The first time a member messages the Bot, they receive an account binding link. After signing in to Patchbay, the platform account is linked to their membership in the current workspace.

Patchbay only runs the agent after binding completes. Every message re-checks account binding and workspace membership; after leaving a workspace, the Bot can no longer be used to reach it.

<Callout type="info">
Account binding only confirms who the sender is. Other members on the chat platform are not automatically added to the Patchbay workspace.
</Callout>

## Manage connections

Workspace owners and admins can connect or disconnect Bots; for Feishu/Lark Bots, connecting and disconnecting is also open to the agent's owner. Regular members can view connected integrations and use the agents they have access to.

After disconnecting, the Bot stops receiving new messages. Existing Patchbay conversations and run records are kept.

## Self-hosting

A self-hosted deployment must configure a 32-byte encryption key for each platform before Patchbay opens its connection entry point:

```dotenv
PATCHBAY_LARK_SECRET_KEY=<base64-encoded 32-byte key>
PATCHBAY_SLACK_SECRET_KEY=<base64-encoded 32-byte key>
PATCHBAY_DINGTALK_SECRET_KEY=<base64-encoded 32-byte key>
PATCHBAY_WECOM_SECRET_KEY=<base64-encoded 32-byte key>
PATCHBAY_TELEGRAM_SECRET_KEY=<base64-encoded 32-byte key>
```

These keys encrypt stored Bot credentials. See [Environment variables](/environment-variables) for how to generate, store, and rotate them. Patchbay Cloud already has this configured.

WeCom's only outbound path is the WebSocket held by one process, so what happens to a reply produced on a *different* replica depends on the realtime relay:

- **Sharded or dual relay mode** (`REDIS_URL` set — the default with Redis): the reply is forwarded to the replica holding the connection and delivered. Multi-replica WeCom is supported.
- **Legacy relay mode, or no Redis**: the reply is dropped. Run the WeCom-enabled backend as a single replica in this configuration.

In every mode, a reply produced while *no* replica holds a live connection (all mid-reconnect) is not delivered. It is counted as `patchbay_wecom_outbound_dropped_total{reason="no_live_connection"}` by the replica that routed it, so the size of that window is measurable; if the loss is unacceptable, a single replica remains the most conservative deployment.

## Next steps

- [Feishu / Lark Bot integration](/lark-bot-integration) — connect an agent by scanning a QR code.
- [Slack Bot integration](/slack-bot-integration) — create a Slack app and paste its tokens into Patchbay.
- [DingTalk Bot integration](/dingtalk-bot-integration) — create a Stream-mode robot and paste its credentials into Patchbay.
- [Telegram Bot integration](/telegram-bot-integration) — create a Bot with @BotFather and paste its token into Patchbay.
- [Chat](/chat) — the conversation and execution model behind every Bot.
