"use client";

import type { ReactNode } from "react";

/** Agent conversations render in GlobalRightSidebar, the shell's sole sidebar. */
export function AgentThreadPanelLayout({ children }: { children: ReactNode }) {
  return children;
}
