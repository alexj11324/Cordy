apiVersion: v2
name: patchbay
description: |
  AI-native task management — like Linear, but with AI agents as first-class citizens.
  Self-host the backend, frontend, and PostgreSQL (pgvector) on a Kubernetes cluster.
type: application
version: 0.1.0
appVersion: "latest"
home: https://patchbay.ai
sources:
  - https://github.com/patchbay-ai/patchbay
keywords:
  - patchbay
  - issue-tracker
  - ai-agents
  - self-host
