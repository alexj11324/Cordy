name: cordy

services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: cordy
      POSTGRES_USER: ${POSTGRES_USER:-cordy}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cordy}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
