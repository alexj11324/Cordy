[package]
name = "cordy-protocol"
description = "WebSocket wire vocabulary shared by server, daemon, and clients — port of server/pkg/protocol"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
